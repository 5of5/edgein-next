table:
  schema: public
  name: vc_firms
array_relationships:
  - name: investments
    using:
      foreign_key_constraint_on:
        column: vc_firm_id
        table:
          schema: public
          name: investments
  - name: investors
    using:
      foreign_key_constraint_on:
        column: vc_firm_id
        table:
          schema: public
          name: investors
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - id
        - logo
        - external_id
        - linkedin
        - name
        - slug
        - website
      backend_only: false
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - logo
        - external_id
        - linkedin
        - name
        - slug
        - website
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - id
        - logo
        - external_id
        - linkedin
        - name
        - slug
        - website
      filter: {}
      check: {}
