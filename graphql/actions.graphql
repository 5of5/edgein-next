
mutation InsertAction($action: String!, $page: String!, $properties: jsonb!, $user: String!) {
  insert_actions_one(
    object: {
      action: $action,
      page: $page,
      properties: $properties,
      user: $user
    }
  ) {
    id
  }
}
